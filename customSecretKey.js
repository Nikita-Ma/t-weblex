
// Config env files

const customSecretKey = {
    node: {
        // add cfg
    },
    db: {
        // add cfg
    },
    jwt: {
        TOKEN_KEY: 'exampleSecret'
    }
}

module.exports = customSecretKey